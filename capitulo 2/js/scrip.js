
        //String
// indexOf("") posicion del caracter en un String;
//toUpperCase(); convertir a mayusculas
//toLowerCase(); convertir   a minusculas
//length sin parentesis (es propiedad solo devulve un valor ) con parentesis () realizan alguna accion
//replace -- remplaza un elemento de un sTring con otro String y se peude encapsular en nueva variable
//substring(inicio,final) trae elementos de la posicion inicio al la posicion "final"
//slice(-indice,-indice) te devuelve desde la posicion contando desde el final hacia atrasar
//trim() elimina los espacios al inicio y al final
 //split() convierte String en un elemento de array
 //split(" ") criterio para separar (por espacios)
//split("") separa si o si  cada elemento sin nungun criterio
//var[indice] -- var es String es tratado tambien como array 


       //Array  (tipo de dato compuesto)
 //var variable = [numero,"string",boolean]
// length
 // variable[indice] = elemento indice +1
 //variable[tamaño+1] = se añade un elemento al vuelo aun si es mayor a su tamaño
 //variable[inidice] = si el indice ya esta ocupado lo reescrive
 //indexOf(indice)= te devuelve el elemento del indice
 //join()--> convierte el array en String
//join("caracter") convierte en un string separado por el caracter
//array1.concat(array2) concatena array1 con array 2
//.unshift(arg1,arg2) agregan elementos al array al inicio
//.push(arg1,arg2) agregan elementos al array al final
//.shift() elimina el primer elemento retorna el elemento que se ah eleiminado
//pop()elimina el ultimo elemento retorna el elemento que se ah eleiminado
//variable.slice(indice ,fin) devuelve un array desde el indice hasta (fin-1) no modificica variable
//splice(indice,elementos) devuelve un array desde el indice hasta numeros de elementos pero elimmina esos elementos del primer variable
 //splice(indice,0,elemeto1) pasando el indice con 0 no elimino nada luego añadimos elemento1 o mas los elementos siguibntes corren
 //splice(indice,elementosquitar,elementosañadir)

//if else case switch do while igual que otros lenguajes de progrmacion
